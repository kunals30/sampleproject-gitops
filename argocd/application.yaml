apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: python-app-nexus
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/<YOUR_GITHUB_USERNAME>/sampleproject-gitops
    targetRevision: main
    path: .
  destination:
    server: https://kubernetes.default.svc
    namespace: sampleproject
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
